server {
    listen {{ nginx_port }};
    server_name localhost;

    location / {
        root {{ app_path }};
        index index.html;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}